[build-system]
requires = ["setuptools>=69", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "haps-isac-distillation"
version = "0.1.0"
description = "Simulator-verified reasoning distillation and constrained control for HAPS-ISAC"
readme = "README.md"
requires-python = ">=3.11,<3.13"
dependencies = [
  "gymnasium>=1.0",
  "numpy>=2.0",
  "pydantic>=2.8",
  "PyYAML>=6.0",
  "scipy>=1.13",
]

[project.optional-dependencies]
ml = [
  "torch>=2.4",
]
data = [
  "h5py>=3.11",
  "pandas>=2.2",
  "pyarrow>=17.0",
]
teacher = [
  "accelerate==1.14.0",
  "Pillow==12.3.0",
  "transformers==5.15.1",
]
student = [
  "bitsandbytes>=0.43",
  "peft>=0.13",
  "transformers==5.15.1",
]
config = [
  "hydra-core>=1.3",
]
dev = [
  "mypy>=1.10",
  "pip-tools>=7.4",
  "pytest>=8.2",
  "pytest-cov>=5.0",
  "ruff>=0.5",
  "types-PyYAML>=6.0",
]

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
addopts = "-ra --strict-config --strict-markers"
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]

[tool.mypy]
python_version = "3.11"
strict = true
packages = ["haps_isac"]
